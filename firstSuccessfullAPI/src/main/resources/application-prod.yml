spring:
  data:
    mongodb:
      uri: "mongodb+srv://newAPI:8529843296@cluster0.h8dks.mongodb.net/journaldb?retryWrites=true&w=majority&appName=Cluster0"
      database: journaldb
      auto-index-creation: true

  main:
    allow-circular-references: true

server:
  port: 8081
  servlet:
    context-path: /journal
